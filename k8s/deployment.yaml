apiVersion: apps/v1
kind: Deployment

metadata:
  name: fraud-api

  namespace: fraud-mlops

spec:

  replicas: 3

  selector:

    matchLabels:

      app: fraud-api

  template:

    metadata:

      labels:

        app: fraud-api

    spec:

      containers:

      - name: fraud-api

        image: ssnall/fraud-mlops:latest

        imagePullPolicy: Always

        ports:

        - containerPort: 8000