apiVersion: v1
kind: ConfigMap

metadata:
  name: prometheus-config

data:
  prometheus.yml: |
    global:
      scrape_interval: 15s

    scrape_configs:

      - job_name: fraud-api

        metrics_path: /metrics

        static_configs:

          - targets:
              - fraud-api-service.fraud-mlops.svc.cluster.local:80